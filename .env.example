# Required by the detector and logits processor. Keep this private in real deployments.
WATERMARK_SECRET=replace-with-a-long-random-secret
# Used only by the optional /generate endpoint.
WATERMARK_MODEL=distilgpt2
